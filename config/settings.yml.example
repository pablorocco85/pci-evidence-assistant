# AI Configuration
ai:
  # Use environment variable SHOPIFY_AI_TOKEN for the token
  base_url: https://proxy-shopify-ai.local.shop.dev  # Local proxy that handles token rotation
  model: gpt-4
  temperature: 0.7
  max_tokens: 1000

# Other settings as before...