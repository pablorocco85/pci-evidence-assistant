# frozen_string_literal: true

# Core dependencies
gem 'roo'                       # Excel file processing
gem 'pdf-reader', '~> 2.11'     # PDF processing
gem 'nokogiri'                  # HTML/XML parsing
gem 'kramdown'                  # Markdown processing
gem 'csv'                       # CSV processing
gem 'logger'                    # Logging
gem 'base64'                    # Base64 encoding/decoding

# Test dependencies
group :test do
  gem 'minitest'                # Testing framework
  gem 'mocha'                   # Mocking and stubbing
end